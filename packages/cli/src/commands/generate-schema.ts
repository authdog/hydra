export const generateSchemaAction = ({
    config
}) => {

    let hydraConfig = {};

    if (config) {
        // validate custom config
    } else {
        // check if config exists
        // validate standard config
    }
    // if valid, set hydraConfig as parsed config

    console.log("Generate Schema action");
}